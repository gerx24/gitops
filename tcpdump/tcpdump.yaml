spec:
  template:
    spec:
      containers:
      - name: tcpdumper
        image: docker.io/dockersec/tcpdump

#kubectl patch deployment  gerx24-core-usva-int-arm -n gerx24 --patch "$(cat tcpdump.yaml)"